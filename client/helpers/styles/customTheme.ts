import { colors } from "..";

export const themeDark = {
  components: {
    Calendar: {
      white: colors.black,
      black: colors.white,
      colorDayText: colors["transparent-gray100"],
      dayHover: colors.beige100,
      dayDisabled: colors["transparent-gray100"],
      borderColor: colors["transparent-gray100"],
    },
    // etc...
  },
};
